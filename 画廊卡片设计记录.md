# 画廊卡片（"Our Best Mistakes"）设计记录

> 复刻 halftone.aura.build 的 "our best mistakes" 页面风格：黄色胶带贴卡片 + 红字黄底标签的拍立得 / 便签风。
> 已用纯 HTML/CSS 实现，并替换进个人网站 `index.html` 的画廊区（原 Coverflow 轮播）。仅本地预览，未上线部署。

---

## 一、文件位置

| 项目 | 位置 |
|------|------|
| 主文件 | `/Users/Andy/Desktop/个人网站/index.html` |
| 样式块 | `.mistake-gallery` 相关（约 249–405 行） |
| 结构 | `<section>` → `.portfolio-inner` → `.mistake-gallery#mistakeGallery` |
| 交互 JS | 页面底部 IIFE（监听 `#mistakeGallery` 内 `.mc-card` 的 `mousemove`） |
| 图片目录 | `pic/作品封面/*.webp` |

---

## 二、7 张卡片当前定稿

| # | 图片文件 | 标题（显示） | 标签 | 外链 |
|---|----------|--------------|------|------|
| 1 | 双铃坠封面 2.webp | 双灵坠 | 动画短片 | bilibili 视频 |
| 2 | 中国古代疆域.webp | 中国古代疆域地图 | 数据可视化 | history.jonlab.cn/maplibre_3d_history.html |
| 3 | 中国古代诗词地图.webp | 中国古代诗词地图 | 数据可视化 | history.jonlab.cn/maplibre_3d_history.html?mode=poem |
| 4 | 玉米黄金美洲豹.webp | 玉米·黄金·美洲豹 | 策略塔防游戏 | 无（用户未提供） |
| 5 | 人物设定图.webp | 人物设定图 | 人物设计 | 无 |
| 6 | 卫星.webp | 近地轨道军事卫星设计图 | 3D 建模 | 无 |
| 7 | 飞船.webp | 星际飞船设计图 | 3D 建模 | 无 |

> 注：卡片 1–3 由 `<a class="mc-card-link">` 包裹（可点击跳转，新窗口打开 + `rel="noopener noreferrer"`）；
> 卡片 4–7 为裸 `<article class="mc-card">`，无外链。

---

## 三、关键样式参数（最终值）

- **画廊容器** `.mistake-gallery`：`display:flex; flex-wrap:wrap; max-width:1180px; gap:64px 60px;`（行距 / 列距）；`padding:50px 0 20px; justify-content:center;`
- **卡片** `.mc-card`：`width:300px; background:#e9e6df;`（暖米灰浅色）`border-radius:16px; padding:16px 16px 22px; position:relative;`
  - 变换用 CSS 变量：`--base / --rx / --ry / --ty / --sc` + `perspective(800px)`
  - `transition: transform .18s ease-out, box-shadow .3s ease; cursor:pointer;`
  - hover（`.mc-card:hover` 与 `.mc-card-link:hover`）：`translateY(-14px) scale(1.05) z-index:100`
- **照片** `.mc-photo`：`aspect-ratio:4/3; overflow:hidden; border-radius:10px;`
  - 暗角：`::after { inset:0; box-shadow: inset 0 0 28px rgba(0,0,0,.14); }`
  - `img { filter: brightness(.97) saturate(1.02); object-fit:cover; }`
- **胶带** `.mc-tape`：`position:absolute; top:-13px; left:27%; width:46%; height:24px; background:rgba(255,230,105,.82); transform:rotate(-3deg); z-index:5;`
- **标签** `.mc-label`：`position:absolute; top:-10px; right:-16px; background:#ffe86e; color:#c23a52; font-size:12px; font-weight:800; letter-spacing:.07em; padding:5px 10px; border-radius:4px; transform-origin:top right; z-index:6; white-space:nowrap;`
- **标签旋转**（7 张，全逆时针、互不相同）：`-14 / -16 / -9 / -12 / -15 / -13 / -11`（deg）
- **卡片整体倾斜** `--base`（7 张）：`-7 / 5 / -4 / 6 / -3 / 4 / -5`（deg）
- **移动端** `@media (max-width:560px)`：`gap:56px 28px; .mc-card { width:100%; max-width:360px; }`

> ⚠️ **选择器陷阱（重要）**：卡片 1–3 由 `<a class="mc-card-link">` 包裹，所以它们的角度 / 标签规则必须写成
> `.mistake-gallery .mc-card-link:nth-child(N) .mc-card` / `.mc-label`；
> 卡片 4–7 是裸 `<article class="mc-card">`，必须写成
> `.mistake-gallery > .mc-card:nth-child(N) .mc-label` / `--base`。
> 两套选择器不可混用，否则对应卡片的角度会回退成 0° / 标签回退成基础 9°，错落效果消失。

---

## 四、3D 鼠标跟随交互

- JS 监听 `#mistakeGallery` 内每张 `.mc-card` 的 `mousemove`；
- 按光标相对卡片中心的坐标设置 CSS 变量 `--rx` / `--ry`（幅度约 ±15°，代码中 `px * 30`）；
- 光标离开时归零；hover 时整卡上浮并轻微放大（见上）；
- 链接卡 hover 时 `z-index:100`，整张（含探出的标签）浮到最上层。

---

## 五、配色决策历程（避免重复踩坑）

1. 初版：纯白米卡 `#fffef9` → 用户觉得黑底上太扎眼；
2. 试过暗色卡 `#131313` → 用户觉得不好看；
3. **最终**：暖米灰 `#e9e6df`（浅色但比纯白柔和），照片加轻暗角降亮；
4. 黄胶带 / 红标签始终保留（暖黄在黑底上更亮眼协调）。

---

## 六、已修复的关键 Bug（备查）

1. **标签被照片裁切**：`.mc-label` 原在 `.mc-photo` 内，而照片有 `overflow:hidden`，探出部分被裁 → 移到 `.mc-card` 直接子元素（与胶带同层）。
2. **`<a>` 包裹后 nth-child 失效**：卡片 4–7 角度 / 标签回退成统一值 → 选择器改为 `.mistake-gallery > .mc-card:nth-child(N)`。
3. **`--base` 规则多写 `.mc-card`**：`.mistake-gallery > .mc-card:nth-child(4) .mc-card` 匹配不到（article 自身即 `.mc-card`）→ 去掉末尾多余 `.mc-card`。
4. **旋转丢单位**：脚本补规则时把 `rotate(12deg)` 写成 `rotate(12)` 导致 CSS 非法 → 正则补回 `deg`。
5. **长标签被相邻卡片遮挡**（几何重叠）：`right:-44px` 探出 + 旋转，最长标签伸出右缘 50px+，而列间距仅 48px，右侧卡片（DOM 靠后）整张盖住标签 → `right:-44px→-16px`，列间距 `48→60px`，字号 `13→12px`。

---

## 七、待确认 / 开放项

- 卡片 6、7 标签「3D 建模」为 AI 猜测，未经用户确认；
- 卡片 4 标签「策略塔防游戏」、卡片 5「人物设计」同上；
- 卡片 4–7 暂无外链（用户未提供），如需可补 `<a class="mc-card-link">` 包裹；
- 标签目前为纯展示文字，未保留原始 halftone 页面的跳转语义。

---

## 八、本地预览

```bash
cd /Users/Andy/Desktop/个人网站
python3 -m http.server 8088 --bind 127.0.0.1
# 浏览器访问 http://127.0.0.1:8088/index.html
```

> 改完 `.html` 后务必校验括号平衡（braces `{}` 数量相等）与标签闭合，单文件超 2800 行，一处多余 `}` 会让整个页面白屏。
